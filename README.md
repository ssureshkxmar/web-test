# SmartECG AI

An advanced standalone AI-powered pipeline to digitize and extract clinical insights from paper ECGs in 3 easy steps.

## Running the Web Portal

To use the AI platform locally:

```bash
pip install -r requirements.txt
python3 app.py
```

Then visit `http://localhost:5001/` in your browser.
# web-test
# web-test
