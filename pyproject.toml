[project]
name = "smartecg-ai"
version = "1.0.0"
description = "Smart AI ECG processing pipeline."
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
